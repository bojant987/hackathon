import actionTypes from '../constants/actionTypes';

const initialState = {
	data: null,
	loading: false,
};

const actionHandlers = {
	[actionTypes.REQUEST_MENU]: (state = initialState, action) => {
		
	},
};